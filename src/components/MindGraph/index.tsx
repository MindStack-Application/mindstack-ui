/**
 * MINDGRAPH COMPONENT
 * 
 * Main component for the MindGraph feature.
 * Shows the graph dashboard by default.
 */

import React from 'react';
import GraphDashboard from './GraphDashboard';

const MindGraph: React.FC = () => {
    return <GraphDashboard />;
};

export default MindGraph;